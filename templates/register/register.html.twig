{% extends 'base.html.twig' %}

{% block body %}
    <div class="flex flex-col items-center h-full">

        <div class="relative flex flex-row items-center space-x-4 pt-12 pb-16">
            <div class="relative">
                <div class="absolute bg-white w-40 p-px ml-1 opacity-60"></div>
                <div class="relative borderGradiantEffect w-40 opacity-90 mt-px pt-1 mr-1"></div>
            </div>
            <h1 class="absolute top-12 right-px">Inscription</h1>
            <h1 class="h1Gradiant">Inscription</h1>
        </div>

        <div class="relative mb-16 w-full h-400 lg:w-600 lg:h-600 px-8 lg:px-0">
            <div class="box register">
                <span></span>
                <span></span>
                <span></span>
                <span></span>
                <form action='/register-add' method='POST' class="register relative w-full h-full flex flex-col justify-center text-center">
                    {% for form in formulaire %}
                        {{form|raw}}
                    {% endfor %}
                    <div class="customSubmit relative">
                        <input type='submit' class="uppercase text-base" value="S'inscrire"/>
                    </div>
                </form>
            </div>
        </div>
    </div>
{% endblock %}

